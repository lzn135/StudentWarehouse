# 运行端口
server:
  port: 8102
#服务器名称
spring:
  application:
    name: eureka-client

#主机地址
eureka:
  client:
    #关闭从注册中心注册服务
    fetch-registry: true
    #指定是否将服务注册到注册中心
    register-with-eureka: true
    service-url:
      defaultZone: http://replica1:8002/eureka/,http:replica2:8003/eureka
